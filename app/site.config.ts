type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Retax",
  site_description: "Retax",
  site_domain: "https://www.retax.in/",
};
